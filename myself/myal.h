#ifndef MYAL_H
#define MYAL_H
#include <vector>

int binarysearch(int k, std::vector<int>& a);

int binomial(int N, int k, int &count, double p = .05);

void select_sort(std::vector<int> &a);

void insert_sort(std::vector<int> &a);

#endif // MYAL_H
